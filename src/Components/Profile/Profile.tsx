import React from 'react';
import {Routes} from "react-router-dom";

const Profile = () => {

    return (
        <div>
           <Routes>

           </Routes>
        </div>
    );
};

export default Profile;